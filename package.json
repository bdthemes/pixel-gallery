{
	"name": "pixel-gallery",
	"version": "2.2.3",
	"main": "index.php",
	"scripts": {
		"dev": "cross-env NODE_ENV=development grunt watch",
		"build": "npm run && npm run clean && cross-env NODE_ENV=production grunt",
		"clean": "rimraf ./assets",
		"zip": "npm run && npm run clean && cross-env NODE_ENV=production grunt && rsync -av --exclude-from=.buildignore . pixel-gallery && zip -r pixel-gallery-v.$npm_package_version.zip pixel-gallery/* && rm -rf pixel-gallery"
	},
	"keywords": [
		"pixel-gallery",
		"theme"
	],
	"author": "bdthemes",
	"license": "ISC",
	"devDependencies": {
		"cross-env": "^7.0.3",
		"grunt": "^1.6.1",
		"grunt-contrib-concat": "^2.1.0",
		"grunt-contrib-copy": "^1.0.0",
		"grunt-contrib-less": "^3.0.0",
		"grunt-contrib-watch": "^1.1.0",
		"grunt-rtlcss": "^2.0.2",
		"grunt-terser": "^2.0.0",
		"jit-grunt": "^0.10.0",
		"rimraf": "^5.0.1",
		"terser": "^5.18.2",
		"uikit": "3.16.23-dev.10a221fa8"
	}
}
